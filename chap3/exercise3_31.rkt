#lang sicp

; if accept-actoin-procedure! would be defined as:
;
; (define (accept-action-procedure!  proc)
;    (set! action-procedures (cons proc action-procedures)))
;
; it would not invoke for initial value of circuit
